# Masking-of-3D-Data
This project selects and masks 3D Numpy files from a hyperspectral camera on the main screen. This masking process also includes brush size selection and refinement, mask clearing, save, and rewind commands. The resulting masked files are saved as png, txt, and npy files.
